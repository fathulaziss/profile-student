import 'package:flutter/material.dart';

class TabLearning extends StatelessWidget {
  const TabLearning({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Learning is Under Development'));
  }
}
